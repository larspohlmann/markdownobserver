# Plan: Swift 6 Strict Concurrency Migration

> Generated by Claude Code — `swift build` with `StrictConcurrency` enabled

## Findings

23 warnings total. 4 critical data races identified.

## Critical Fixes

- [x] `ImageCache` — convert `class` to `actor` (data race on `NSCache`)
- [x] `APIRouter` — make `baseURL` immutable, isolate headers
- [ ] `NotificationManager` — remove `@unchecked Sendable`, fix underlying race
- [ ] `SyncCoordinator` — replace `DispatchQueue.global()` with `Task {}`

## Remaining Work

- [ ] Audit 7 `@unchecked Sendable` conformances
- [ ] Replace 5 remaining `DispatchQueue.global()` calls
- [ ] Enable `StrictConcurrency` in `Package.swift`
- [ ] Run full test suite, fix remaining warnings
